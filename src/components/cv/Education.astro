---
import { education } from "@cv";
import Section from "@/components/cv/Section.astro"
---
<Section title="Education">
  <h2 class="text-xl font-bold mt-8">Formación</h2>
  <div class="flex flex-col gap-y-4">
    {education.map(({ institution, startDate, endDate, area}) => {
      return (
        <div class="flex flex-col gap-y-1">
        <h3 class="text-base font-semibold">{institution}</h3>
        <p class="font-mono text-sm text-pretty text-foreground/80 print:text-[12px]">{area}</p>
        </div>
      )
    })
    }
  </div>
</Section>